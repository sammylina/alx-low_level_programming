#include <string.h>

/**
 * _strlen - find the length os a string
 * @s: string type
 *
 * Description: the fuction takes one string
 * and count howmany chars it has
 * Return: The numbers of chars
 */

int _strlen(char *s)
{
	return (strlen(s));
}
